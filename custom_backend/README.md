# Custom backend

Flask backend scaffold for the custom Stars/Premium/gifts features.

Environment variables:
- ADMIN_USER_ID
- GIFT_CHANNEL_URL
- GIFT_CHANNEL_ID
- TELEGRAM_BOT_TOKEN
- PORT (optional, defaults to 10000)

Install dependencies with `pip install -r requirements.txt` and run `python server.py`.
